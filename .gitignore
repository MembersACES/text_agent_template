# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# local chat trace logs (per-message backend + response)
.local/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
# Allow .env.example to be committed
!.env.example

# local test inputs holding REAL customer data (order numbers + emails) â€” never commit.
# Templates named *.example.json ARE committed.
*.local.json

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# local scratch — not part of the build
_to_delete/
test-entry.ts
test.mjs

# Client correspondence and commercials. NEVER commit: these carry real customer
# email addresses and fee/margin notes. Listed so `git add -A` cannot leak them.
HTG_email_chain.md
HTG_order_tracking_scope_and_cost.md
HTG_project_instructions.md
HTG_Order_Tracking_Wording_for_Signoff.docx
HTG_reply_to_Iri*.md
HTG_email_Iri*.md
MachShip-Handoff.md
MachShip-Meeting-Script-16Jul.md
Order-Tracking-Scope-Expansion-4Aug.md
Order-Tracking-Scope-Iri-3Aug.md
